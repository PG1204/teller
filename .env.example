# Copy to .env (gitignored). Never commit real keys.

# --- LLM provider for the discovery run (replay never needs a key) ---
# gemini | anthropic
TELLER_LLM_PROVIDER=gemini
GEMINI_API_KEY=
TELLER_GEMINI_MODEL=gemini-3.8-flash
ANTHROPIC_API_KEY=
TELLER_ANTHROPIC_MODEL=claude-opus-5

# --- Mock target app credentials (synthetic; the model never sees them) ---
LEDGERLINE_USER=teller1
LEDGERLINE_PASS=Ledger!2026
LEDGERLINE_IDLE_SECONDS=600

# --- Operator identity shown in handoff records ---
TELLER_OPERATOR=operator
